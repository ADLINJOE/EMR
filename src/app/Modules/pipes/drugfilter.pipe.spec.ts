import { DrugfilterPipe } from './drugfilter.pipe';

describe('DrugfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DrugfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
